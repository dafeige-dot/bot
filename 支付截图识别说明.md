# 支付截图识别功能说明

## ✅ 功能概述

Bot 现在可以智能识别支付截图（Google Pay、PhonePe、Paytm等），自动提取：

1. **💰 金额** (Amount)
2. **🔢 UTR** (UPI Transaction Reference - 12位数字)
3. **📱 UPI** (VPA地址，如 ppqr01.kwjczm@iob)

识别到 UPI 后，会自动调用后端接口验证 UPI 是否属于我们。

---

## 🎯 使用场景

### 场景 1：客户发送支付截图

**操作**：
客户直接发送支付截图到 Bot

**Bot 自动处理**：
1. OCR 识别截图内容
2. 提取金额、UTR、UPI
3. 调用 `/check_upi` 接口查询
4. 返回验证结果

---

## 📸 支持的截图类型

### Google Pay 截图
```
✅ 支持识别:
- 金额: ₹500
- UTR: 566885730682
- UPI: ppqr01.kwjczm@iob
- 付款人: PRAVIN MANOHAR HARINKHEDE
- 银行: State Bank of India
```

### PhonePe / Paytm / 其他 UPI 应用
```
✅ 支持识别:
- 金额
- UTR/Transaction ID
- UPI 地址
- 付款人信息
```

---

## 🔍 识别逻辑

### 1. 金额识别

支持多种格式：
- `₹500`
- `Rs 500`
- `INR 500`
- `500 rupees`
- `500.00`

### 2. UTR 识别

识别模式：
- 12位数字（标准UTR）
- 10-15位数字
- 包含 "UPI transaction ID" 等关键词

### 3. UPI 识别

识别模式：
- VPA 格式: `xxx@xxx`
- 例如: `merchant@paytm`, `user@ybl`, `ppqr01.kwjczm@iob`
- 自动过滤邮箱地址

---

## 💬 Bot 响应示例

### 示例 1：UPI 是我们的

发送截图后，Bot 返回：

```
🧾 识别到支付信息：

💰 金额: ₹500
🔢 UTR: 566885730682
📱 UPI: ppqr01.kwjczm@iob
👤 付款人: PRAVIN MANOHAR HARINKHEDE
🏦 银行: State Bank of India

==============================

✅ 这是我们的 UPI 地址

⚠️ 但是 UTR 566885730682 可能:
• UTR 不正确
• 或已被其他商户领取

📞 请联系客服确认
```

### 示例 2：UPI 不是我们的

```
🧾 识别到支付信息：

💰 金额: ₹500
🔢 UTR: 566885730682
📱 UPI: wrongupi@paytm

==============================

❌ 这不是我们的 UPI 地址

⚠️ 请确认:
• UPI 地址是否正确
• 是否转错账户

📞 如有疑问请联系客服
```

### 示例 3：只有金额和 UTR，没有 UPI

```
🧾 识别到支付信息：

💰 金额: ₹500
🔢 UTR: 566885730682
👤 付款人: JOHN DOE

⚠️ 未找到 UPI 地址
💡 如需查询订单，请发送订单号
```

---

## 🔧 技术实现

### 1. OCR 识别

使用 PaddleOCR 或云OCR识别图片文字

### 2. 信息提取

`app/utils/payment_ocr.py` - 专门的支付信息解析器

```python
class PaymentOCRParser:
    - extract_amount()  # 提取金额
    - extract_utr()     # 提取UTR
    - extract_upi()     # 提取UPI
    - extract_bank_name()  # 提取银行
    - extract_sender_name()  # 提取付款人
```

### 3. UPI 验证

调用后端接口：
- **接口**: `POST /httpapi/telegram/check_upi`
- **参数**: `merchant_id`, `upi`
- **返回**: `{ "code": 200, "is_upi": 0/1 }`

### 4. 智能提示

根据 `is_upi` 返回值：
- `is_upi = 1`: UPI 是我们的，提示确认 UTR
- `is_upi = 0`: UPI 不是我们的，提示转错账户

---

## 📋 完整工作流程

```
用户发送支付截图
    ↓
OCR 识别图片文字
    ↓
提取支付信息
(amount, utr, upi, bank, sender)
    ↓
识别到 UPI？
    ├─ 是 → 调用 check_upi 接口
    │         ↓
    │    is_upi = 1?
    │    ├─ 是 → "这是我们的 UPI，请确认 UTR"
    │    └─ 否 → "这不是我们的 UPI"
    │
    └─ 否 → 尝试查找订单号
              ├─ 找到订单号 → 查询订单
              └─ 没找到 → 显示识别到的信息
```

---

## 🧪 测试用例

### 测试 1：完整的 Google Pay 截图

**预期识别**：
- 金额: ✅
- UTR: ✅  
- UPI: ✅
- 付款人: ✅
- 银行: ✅

**预期结果**：
调用 UPI 查询，返回验证结果

### 测试 2：只有 UTR 没有 UPI

**预期识别**：
- UTR: ✅
- UPI: ❌

**预期结果**：
显示 "未找到 UPI 地址"

### 测试 3：普通订单截图

**预期识别**：
- 订单号: ✅
- UPI: ❌

**预期结果**：
正常查询订单

---

## ⚙️ 配置说明

### 后端 API 配置

`.env` 文件：
```bash
# 后端 API 地址
BACKEND_API_URL=http://127.0.0.1:60010/httpapi/telegram

# 超时时间
BACKEND_API_TIMEOUT=10
```

### OCR 配置

```bash
# OCR 引擎
OCR_ENGINE=paddleocr

# 是否启用OCR
ENABLE_OCR=true
```

---

## 🆕 新增接口

### check_upi 接口

**请求**：
```
POST /httpapi/telegram/check_upi

参数:
- merchant_id: 商户号
- upi: UPI地址
```

**响应**：
```json
{
  "code": 200,
  "msg": "success",
  "is_upi": 1,  // 1=是我们的, 0=不是我们的
  "data": {
    // 其他数据
  }
}
```

---

## 📝 注意事项

### 1. OCR 准确性

- 清晰的截图识别率更高
- 模糊的截图可能识别失败
- 建议用户发送高清原图

### 2. UPI 格式

- 标准格式: `username@bank`
- 会自动过滤邮箱地址
- 只识别 UPI VPA 地址

### 3. UTR 格式

- 标准为12位数字
- 也支持10-15位数字
- 必须是纯数字

### 4. 错误处理

- 识别失败会提示用户
- API 调用失败不会影响 Bot 运行
- 所有异常都有日志记录

---

## 🚀 快速开始

### 1. 确保后端接口可用

测试 check_upi 接口：
```bash
curl -X POST http://127.0.0.1:60010/httpapi/telegram/check_upi \
  -d "merchant_id=MERCHANT001" \
  -d "upi=ppqr01.kwjczm@iob"
```

### 2. 启动 Bot

```bash
start.bat
```

### 3. 测试功能

发送支付截图到 Bot，查看识别结果

---

## 🔍 调试日志

### 识别成功

```
开始解析支付截图 OCR 文本
提取到金额: 500
提取到 UTR: 566885730682
识别到 UPI: ppqr01.kwjczm@iob
解析结果: {'amount': '500', 'utr': '566885730682', 'upi': 'ppqr01.kwjczm@iob', ...}
开始 UPI 查询: UPI=ppqr01.kwjczm@iob, UTR=566885730682, Amount=500
UPI 查询完成: is_upi=1
```

### 识别失败

```
开始解析支付截图 OCR 文本
解析结果: {'amount': None, 'utr': None, 'upi': None, ...}
未识别到订单号
```

---

完成时间: 2025-11-07

