# Telegram Bot
python-telegram-bot==20.7
python-telegram-bot[webhooks]==20.7

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.0

# Redis (celery[redis] 会自动安装兼容版本的 redis 包，无需单独指定)
hiredis==2.2.3

# OCR（Docker/Linux 默认不安装 PaddleOCR，避免 PyMuPDF 冲突）
# 如需在 Windows 本机启用，请取消下行注释中的平台条件
paddleocr==2.7.0.3; sys_platform == 'win32' and python_version < '3.11'
paddlepaddle>=2.6.1; sys_platform == 'win32' and python_version < '3.11'
opencv-python-headless>=4.8.0
Pillow>=10.0.0
# 仅在 Windows 环境使用较新的 PyMuPDF 以获取预编译轮子，避免编译失败
PyMuPDF>=1.23.0; sys_platform == 'win32'

# Linux OCR（Tesseract）
pytesseract>=0.3.10

# 腾讯云 OCR SDK（高识别率，推荐）
tencentcloud-sdk-python>=3.0.1000

# 纯本地 OCR（无需云）：RapidOCR（ONNXRuntime）
rapidocr-onnxruntime>=1.3.18

# 工具库
python-dotenv==1.0.0
loguru==0.7.2
pytz==2023.3.post1
python-dateutil==2.8.2

# HTTP客户端
httpx==0.25.2
aiohttp==3.9.1

# 数据验证和序列化
marshmallow==3.20.1

# 安全
cryptography==41.0.7
python-jose[cryptography]==3.3.0

# 监控和日志
prometheus-client==0.19.0
sentry-sdk==1.39.1

# 开发工具
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
black==23.12.0
flake8==6.1.0
mypy==1.7.1

